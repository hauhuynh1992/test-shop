package com.example.quiz.models

enum class ShopHopTrackStatus {
    PENDING,
    DELIVERED,
    SHIPPED,
    CANCELLED,
    ARRIVED,
    OUTFORDELIVERY,
    NOTDONE,
    DELIVERING,
    pending, processing, on_hold, completed, cancelled, refunded, failed , trash
}